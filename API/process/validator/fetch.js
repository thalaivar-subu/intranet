import validateCreate from "./create";

const validateFetch = (requestRoute, requestData) => {
  let validationResult = { isValid: true };
  if (requestRoute.startsWith("/info-routes")) {
  }
  return validationResult;
};

export default validateFetch;
